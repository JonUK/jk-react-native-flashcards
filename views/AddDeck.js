import React from 'react';
import { Text, View } from 'react-native';
import globalStyles from '../utils/globalStyles';

export default function AddDeck() {

  return (
    <View styles={globalStyles.viewContainer}>
      <Text>Add deck</Text>
    </View>
  );

}

